"""Seurat-RPCA via rpy2 bridge — placeholder."""

from __future__ import annotations
import numpy as np


def run(adata, batch_key: str, seed: int) -> np.ndarray:
    raise NotImplementedError(
        "Seurat-RPCA wrapper needs rpy2 + Seurat; installing next cycle."
    )
